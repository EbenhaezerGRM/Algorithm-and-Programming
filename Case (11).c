#include <stdio.h>

int main(){
    int T;
    
    scanf("%d", &T);
    long long int N[T], A[T], B[T], C[T], D[T];

    long long int dibuang = 0;
    for(int i = 1; i <= T; i++){
        scanf("%lld %lld %lld %lld %lld", &N[i], &A[i], &B[i], &C[i], &D[i]);
    }

    for(int j = 0; j < T; j++){
        if(N[T] %)
    }
    return 0;
}

